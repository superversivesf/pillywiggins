# Personalities

> **Canonical personality files live in [`personalities/`](../../personalities/) at the repo root.**
> This directory is a historical artifact — the old `.json` format files were removed in May 2026.
> See `personalities/` for the current YAML packs: `fey_court/`, `bridge/`, `clinic/`, `studio/`, `tavern/`, `kitchen/`, `ship/`, `study/`, `workshop/`.
