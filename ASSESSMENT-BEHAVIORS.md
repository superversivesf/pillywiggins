# Behavior Assessment: Pillywiggins

> Generated by swarm worker `behavior-mapper` on 2026-04-27.
> Scope: inspect `src/pillywiggins/adapters/`, `agents/`, `skills/`, `memory/`, `messaging/`, `scheduling/`, `personalities/`, and top-level `skills/` directory.

---

## 1. Adapters

| Platform | Adapter File Exists | `UnifiedMessage` Producer | Status |
|----------|--------------------|--------------------------|--------|
| **Discord** | `src/pillywiggins/adapters/discord_adapter.py` (239 lines) | Yes — `normalize(raw_message: discord.Message)` produces `UnifiedMessage` with `ChannelType.DISCORD`, metadata (`username`, `is_bot`, `guild_id`), and `conversation_key = channel.id`. | **Functional** |
| **Telegram** | `src/pillywiggins/adapters/telegram_adapter.py` (245 lines) | Yes — `normalize(raw_update: Update)` produces `UnifiedMessage` with `ChannelType.TELEGRAM`, metadata (`username`, `is_bot`, `chat_id`), and `conversation_key = chat.id` or `from_user.id` for groups. | **Functional** |
| **Slack** | ✅ `slack_adapter.py` exists (233 lines) | Yes — `normalize()` produces `UnifiedMessage` with `ChannelType.SLACK`. Same command set as Discord/Telegram. | **Functional** |
| **Matrix** | ✅ `matrix_adapter.py` exists (225 lines) | Yes — `normalize()` produces `UnifiedMessage` with `ChannelType.MATRIX`. Same command set. | **Functional** |
| **Email** | ✅ `email_adapter.py` exists (340 lines) | Yes — `normalize()` produces `UnifiedMessage` with `ChannelType.EMAIL`. Same command set. | **Functional** |

### Adapter Capabilities (Discord & Telegram)
Both functional adapters implement the full `BaseAdapter` contract:
- `connect()` / `listen()` / `send()` / `normalize()`
- Commands: `!help` / `!status` / `!models` / `!model <name>` / `!skills` / `!compact` / `!reset`
- User authorization via `allowed_user_ids` (`_is_authorized`)
- Bot-to-bot chat throttling (`_should_respond_to_bot` with configurable `bot_chat_limit`)
- Typing indicator while generating (`_keep_typing`)
- Graceful shutdown on SIGINT/SIGTERM

---

## 2. Agent Runtime (`PillywigginAgent`)

| Capability | Status | Evidence |
|-----------|--------|----------|
| **Turn-based concurrency** | **Functional** | `self._lock = asyncio.Lock()` acquired in `handle_message()` (`agents/base.py:381`) |
| **PydanticAI brain integration** | **Functional** | `create_brain()` returns `pydantic_ai.Agent` with `deps_type=AgentDeps`, system prompt injected via `@agent.system_prompt`, 12 built-in tools + dynamic skill tools registered (`agents/brain.py:577-648`) |
| **Agent lifecycle (`start` / `shutdown`)** | **Functional** | Connects council memory, NATS bus, scheduler; graceful teardown pops from `_ACTIVE_AGENTS` and closes all resources (`agents/base.py:180-242`) |
| **Conversation history split** | **Functional** | `_conversation_histories: dict[str, list[ModelMessage]]` keyed by `conversation_key`; `_message_history` for default key |
| **Model switching** | **Functional** | `switch_model()` rebuilds brain via `create_brain()`; adapter commands `!model` call it dynamically |
| **History compaction** | **Functional** | `compact_history()` summarizes old messages via LLM, truncates remaining parts, persists back to history |
| **History clearing** | **Functional** | `clear_history()` deletes in-memory + persists empty state to Redis cache and PostgreSQL store |
| **Conversation serialization** | **Functional** | `ModelMessagesTypeAdapter.dump_json()` / `.validate_json()` used by `ConversationCache` and `ConversationStore` |

---

## 3. Built-In Tools (Agent Brain)

All tools are registered in `create_brain()` (`agents/brain.py:629-642`) and are **functional**:

| Tool | Description |
|------|-------------|
| `recall_private_memory` | Embeds query via `embed()`, searches `PrivateMemory` (pgvector) |
| `save_to_private_memory` | Embeds content, saves to `PrivateMemory` |
| `query_council_memory` | Embeds query, searches `CouncilMemory` (shared pgvector) |
| `share_to_council` | Embeds insight, writes to `CouncilMemory`, broadcasts via NATS `insight` topic |
| `build_skill` | Creates a `SkillDraft` via `draft_skill()`; validates code |
| `test_skill_code` | Runs test cases in sandbox via `test_skill()` |
| `review_skill_code` | Formats code + test results for user review |
| `deploy_skill_code` | Deploys skill file + registry entry + NATS broadcast if approved and tests pass |
| `schedule_task` | Adds APScheduler job via `AgentScheduler.add_job()` (interval or cron) |
| `unschedule_task` | Removes APScheduler job by name |
| `list_scheduled_tasks` | Lists all jobs with names, actions, next run times |
| `send_message_to_agent` | Sends `UnifiedMessage` via NATS direct publish to `council.direct.{target_agent_id}` |
| `get_current_time` | Returns current time in agent's configured timezone (from personality) |
| `get_conversation_info` | Returns message count + estimated token count for current conversation |

**Dynamic skill tools**: `create_brain()` iterates `skill_registry.list_skills()` and wraps each in a PydanticAI tool via `_make_skill_tool()` (including sandboxed execution when configured).

---

## 4. Skills System

| Component | Status | Evidence |
|-----------|--------|----------|
| **`SkillRegistry.load_all()`** | **Functional** | Loads `*.py` from `skills/` directory, validates `SKILL_META` + `run()` function, registers `Skill` objects. Called on agent init and on NATS `skill_deployed` message (`skills/registry.py:35-52`) |
| **`SkillRegistry.register_skill()`** | **Functional** | Writes `.py` file to `skills_dir`, updates `registry.json`, reloads into in-memory registry (`skills/registry.py:91-109`) |
| **`SkillRegistry` file watching (auto-reload)** | **Stubbed** | No `watchdog`, `inotify`, or polling loop exists. Skills only reload on agent restart or on `skill_deployed` NATS broadcast. |
| **Sandbox (`run_sandboxed`)** | **Functional** | Spawns `asyncio.create_subprocess_exec` with `sys.executable`, restricted env (denies `DATABASE`, `SECRET`, `TOKEN`, etc.), configurable timeout (default 30s), JSON wrapper template. Returns `SandboxResult` (`skills/sandbox.py:74-175`) |
| **Skill builder — `draft_skill`** | **Functional** | Validates AST, requires `SKILL_META` + `run()`, blocks dangerous patterns (`os.system`, `eval`, `exec`, etc.) unless `subprocess` permission granted (`skills/builder.py:87-99`) |
| **Skill builder — `test_skill`** | **Functional** | Runs each test case through sandbox, compares expected/actual, populates `SkillDraft.test_results` (`skills/builder.py:102-158`) |
| **Skill builder — `review_skill`** | **Functional** | Formats code + test results + approval prompt (`skills/builder.py:161-193`) |
| **Skill builder — `deploy_skill`** | **Functional** | Checks `approved=True`, status >= `TESTED`, no failing tests; calls `registry.register_skill()`; broadcasts `skill_deployed` via NATS (`skills/builder.py:195-227`) |
| **`generate_skill_boilerplate`** | **Functional** | Produces template with `SKILL_META`, `async def run(**kwargs)`, logging, try/except (`skills/templates.py:56-72`) |
| **`validate_skill_code`** | **Functional** | AST-level checks: syntax, SKILL_META exists, `async def run`, `**kwargs` param, try/except block, `logging.getLogger` call (`skills/templates.py:94-179`) |
| **URL filter** | **Functional** | `is_safe_url()` blocks private IP ranges (`skills/url_filter.py`) |
| **Pre-built skills** | **Functional** | `web_search.py` (SearXNG), `check_website.py`, `count_words.py`, `roll_dice.py` |

---

## 5. Memory

### Private Memory (`memory/private.py`)
| Feature | Status |
|---------|--------|
| `connect()` with `set_config('app.agent_id', ...)` | **Functional** |
| `save(content, embedding, metadata)` | **Functional** |
| `search(query_embedding, limit=5)` with cosine similarity | **Functional** |
| `delete(memory_id)` | **Functional** |

### Council Memory (`memory/council.py`)
| Feature | Status |
|---------|--------|
| `connect()` | **Functional** |
| `write_entry()` with validation | **Functional** — validates `MAX_CONTENT_LENGTH` (2000), message type whitelist, tag whitelist (`TAG_WHITELIST`), rate limit (`RATE_LIMIT_PER_HOUR` = 10), dedup via cosine similarity (`DEDUP_SIMILARITY_THRESHOLD` = 0.95) |
| `search(query_embedding, tags, limit)` with tag filtering | **Functional** |
| `delete_entry(entry_id)` | **Functional** |
| `get_entry(entry_id)` | **Functional** |
| `list_entries(limit, offset)` | **Functional** |

### Conversation Cache (`memory/cache.py`)
| Feature | Status |
|---------|--------|
| Redis-backed `save()` / `load()` with 30-min TTL | **Functional** |
| `ModelMessagesTypeAdapter` serialization | **Functional** |

### Conversation Store (`memory/store.py`)
| Feature | Status |
|---------|--------|
| PostgreSQL-backed `save()` / `load()` with JSONB, upsert (`ON CONFLICT`) | **Functional** |
| RLS enforcement via `set_config('app.agent_id', ...)` on connection init | **Functional** |

### Database Schema (`scripts/init-db.sql`)
| Feature | Status |
|---------|--------|
| `private_memory` table with `vector(768)`, `ivfflat` index | **Functional** |
| `council_memory` table with `vector(768)`, `ivfflat` index, `superseded_by` FK | **Functional** |
| `conversation_cache` table with JSONB, unique `(agent_id, channel, conversation_key)` | **Functional** |
| RLS enabled + policies on `private_memory` and `conversation_cache` | **Functional** |

> **Note**: `council_memory` table does **not** have RLS (intentional — shared read). Write isolation is enforced by application-level validation in `CouncilMemory.write_entry()`, not by a database policy.

---

## 6. Messaging (`messaging/nats_bus.py`)

| Feature | Status | Evidence |
|---------|--------|----------|
| **NATS connection** | **Functional** | `nats.connect(servers=[self._nats_url])`, JetStream init |
| **JetStream stream creation** | **Functional** | `add_stream()` for `COUNCIL_STREAM` with subjects `council.broadcast` and `council.direct.>` |
| **`publish_broadcast(msg_type, data)`** | **Functional** | Publishes to `council.broadcast` |
| **`publish_direct(target_agent_id, msg_type, data)`** | **Functional** | Publishes to `council.direct.{target_agent_id}` |
| **`subscribe_broadcast(handler)`** | **Functional** | Durable JetStream subscription with queue name `pillywiggins-{agent_id}-broadcast`; callback receives `(msg_type, data)` |
| **`subscribe_direct(handler)`** | **Functional** | Durable JetStream subscription on `council.direct.{agent_id}` with queue name `pillywiggins-{agent_id}-direct` |
| **`close()`** | **Functional** | Drains + closes NATS connection, clears subscription list |

### UnifiedMessage (`messaging/unified.py`)
- `ChannelType` enum: `TELEGRAM`, `DISCORD`, `SLACK`, `MATRIX`, `EMAIL` — **Functional**
- `UnifiedMessage` dataclass with `channel`, `channel_user_id`, `content`, `conversation_key`, `timestamp`, `metadata` — **Functional**

---

## 7. Scheduling (`scheduling/scheduler.py`)

| Feature | Status | Evidence |
|---------|--------|----------|
| **APScheduler + RedisJobStore** | **Functional** | Parses Redis URL, creates `RedisJobStore` with `db` parameter; falls back to `MemoryJobStore` on connection failure |
| **AsyncIOScheduler** | **Functional** | `AsyncIOScheduler(jobstores=jobstores)` started in `AgentScheduler.start()` |
| **YAML schedule loading** | **Functional** | `_load_schedules()` reads from personality YAML; supports `interval_seconds` and `cron` |
| **JSON schedule persistence** | **Functional** | `_persist_json_job()` / `_remove_json_job()` / `_load_json_schedules()` manage `{agent_id}_schedules.json` |
| **Cron expression parsing** | **Functional** | `parse_cron()` splits 5-field cron into `minute`, `hour`, `day`, `month`, `day_of_week` for APScheduler trigger |
| **Builtin handlers**
|   `heartbeat` | **Functional** | Just logs; agent overrides with real NATS broadcast via `_builtin_heartbeat_handler` |
|   `send_message` | **Functional** | Agent overrides with real LLM + adapter send via `_builtin_send_message_handler` |
|   `memory_review` | **Stubbed** | Logs only (`logger.info("memory review for %s", agent_id)`); no actual memory review logic |
|   `skill_reload` | **Stubbed** | Logs only (`logger.info("skill reload for %s", agent_id)`); no actual skill directory scan/force reload logic |
|   `custom` | **Stubbed** | Logs only; generic placeholder |
| **`add_job()`** (interval or cron) | **Functional** | Tool `schedule_task` drives this; auto-sets `conversation_key` for `send_message` actions |
| **`remove_job()`** | **Functional** | Tool `unschedule_task` drives this |
| **`list_jobs()`** | **Functional** | Tool `list_scheduled_tasks` drives this |
| **`misfire_grace_time=300`** | **Functional** | Set on every scheduled job |

---

## 8. Personality Loader (`agents/personality.py`)

| Feature | Status | Evidence |
|---------|--------|----------|
| **`load_personality(path)`** | **Functional** | `yaml.safe_load()`, supports both old schema (`description`, `system_prompt`, `traits`) and new schema (`archetype`, `tone`, `style`, `response_length`, `additional_instructions`). Auto-derives missing old fields from new schema. |
| **`Personality.build_system_prompt()`** | **Functional** | Assembles prompt from whichever fields are present, injects timezone dynamically |
| **YAML schedule parsing** | **Functional** | `schedules` list parsed into `Personality.schedules`; `scheduling` dict parsed into `Personality.scheduling` |
| **`bot_chat_limit` parsing** | **Functional** | Supports integer, `"unlimited"` string (→ -1), defaults to 3 |
| **Timezone parsing** | **Functional** | Defaults to `"UTC"`; passed to agent scheduler and `get_current_time` tool |
| **Personality files present** | **Functional** | 31 YAML files in `personalities/` (e.g., `puck.yaml`, `acorn.yaml`, `beekeeper.yaml`, etc.) |

---

## 9. What Works Right Now vs What Is Stubbed

### Functional (implemented and operational)

1. **Discord adapter** — full message listening, normalization, command prefix handling, authorization, typing indicator, graceful shutdown.
2. **Telegram adapter** — full polling bot, command handling, message normalization, authorization, typing indicator, graceful shutdown.
3. **`UnifiedMessage` producer** — both Discord and Telegram adapters correctly normalize raw platform messages into `UnifiedMessage`.
4. **`PillywigginAgent` turn-based concurrency** — `asyncio.Lock` in `handle_message()`.
5. **PydanticAI brain integration** — `Agent` with injected `AgentDeps`, dynamic personality system prompt, 12+ built-in tools.
6. **Built-in tools** — all 12+ tools in `brain.py` are fully implemented and wired to real backends (memory, scheduling, NATS, skills).
7. **Dynamic skill tool registration** — skills loaded from `skills/*.py` are dynamically wrapped and registered as PydanticAI tools at brain creation time.
8. **`SkillRegistry.load_all()`** — loads all `.py` files from disk, validates `SKILL_META` and `run()` signature.
9. **`SkillRegistry.register_skill()`** — writes file to disk, updates `registry.json`, and loads into memory.
10. **Skill sandbox** — subprocess isolation with restricted env, timeout (default 30s), JSON result wrapper, permission gating.
11. **Skill builder flow** — `draft_skill` → `test_skill` → `review_skill` → `deploy_skill` with AST validation and dangerous-pattern detection.
12. **Skill boilerplate generator** — `generate_skill_boilerplate()` produces conformant scaffold.
13. **Skill code validator** — `validate_skill_code()` checks AST, `SKILL_META`, `async def run`, `**kwargs`, try/except, logging.
14. **Private memory** — PostgreSQL + pgvector + RLS (`set_config('app.agent_id')`), save/search/delete via `PrivateMemory`.
15. **Council memory** — PostgreSQL + pgvector, write validation (length, tags, rate limit, dedup), search by embedding + tag filter.
16. **Conversation cache (Redis)** — `ConversationCache` with `ModelMessagesTypeAdapter`, 30-min TTL.
17. **Conversation store (PostgreSQL + RLS)** — `ConversationStore` with JSONB upsert, RLS via `app.agent_id`.
18. **Database initialization schema** — `scripts/init-db.sql` creates all three tables, indexes, and RLS policies.
19. **NATS connection and stream setup** — connects, creates `COUNCIL` JetStream stream on demand.
20. **`publish_broadcast` / `subscribe_broadcast`** — council-wide heartbeat and insight broadcasts.
21. **`publish_direct` / `subscribe_direct`** — agent-to-agent direct messaging on `council.direct.{agent_id}`.
22. **APScheduler + RedisJobStore** — per-agent scheduler with Redis persistence, fallback to in-memory.
23. **Cron schedule support** — 5-field cron parsed and passed to APScheduler `CronTrigger`.
24. **YAML schedule loading from personality** — schedules defined in `puck.yaml`, `acorn.yaml`, etc. are loaded at agent start.
25. **JSON schedule persistence** — runtime-added jobs survive scheduler restarts via `{agent_id}_schedules.json`.
26. **Personality YAML loader** — dual-schema support, timezone, bot chat limit, schedule parsing.
27. **`get_current_time` tool** — uses personality timezone via `ZoneInfo`.
28. **`get_conversation_info` tool** — counts messages and estimates tokens.
29. **Model switching** — `switch_model()` rebuilds brain; `!model` and `/model` commands work in both adapters.
30. **History compaction** — LLM-based summarization of old messages with truncation of recent messages.
31. **History clearing** — deletes in-memory + persists empty state to Redis and PostgreSQL.
32. **Bot chat limit / throttling** — configurable per-channel, with separate counts per `conversation_key`.
33. **User authorization** — `allowed_user_ids` parsed from env, supports `"all"`.
34. **Health check server** — `/healthz` endpoint checks Postgres, Redis, Ollama, NATS.
35. **Embeddings generation** — `embed()` with retry/backoff, Ollama and OpenAI-compatible endpoints, in-memory SHA256 cache (1-hour TTL).
36. **`web_search` skill** — functional SearXNG integration with categories, engines, result limiting.
37. **`check_website` skill** (top-level `skills/`)
38. **`count_words` skill** (top-level `skills/`)
39. **`roll_dice` skill** (top-level `skills/`)
40. **Onboard wizard** (`pillywiggins onboard`) — interactive agent creation, token validation, LLM model polling, Docker Compose generation, `.env` management.
41. **`agents.yaml` multi-agent config** — `agents_config.py` loads multi-agent definitions with env expansion.
42. **URL safety filter** — `is_safe_url()` blocks private/internal IP ranges.

### Stubbed (not implemented or incomplete)

1. **Skill registry file watching** — no `watchdog`, `inotify`, or polling mechanism. Skills only reload on agent restart or on NATS `skill_deployed` broadcast. The `skill_reload` builtin scheduler handler only logs; it does not call `skill_registry.load_all()`.
2. **Scheduler builtin `memory_review`** — only logs (`logger.info("memory review for %s", agent_id)`). No actual memory consolidation or pruning logic.
3. **Scheduler builtin `skill_reload`** — only logs (`logger.info("skill reload for %s", agent_id)`). Does not call `SkillRegistry.load_all()` to refresh the filesystem.
4. **Scheduler builtin `custom`** — generic placeholder; logs args but takes no real action beyond the log line.

---

## Summary Count

| Category | Count |
|----------|-------|
| **Functional behaviors** | 45 |
| **Stubbed behaviors** | 4 |
| **Total behaviors mapped** | **49** |

---
*End of assessment.*
